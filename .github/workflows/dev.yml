name: Deploy Spring Boot

on:
  push:
    branches:
      - dev
jobs:
  deploy:
    runs-on: [self-hosted, Dev-Server]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
      - name: Grant Gradle Permission
        run: chmod +x ./gradlew
      - name: Build
        run: ./gradlew clean bootJar -x test
      - name: Deploy
        run: |
          mkdir -p /home/hskgroup-ubuntu/hskgroup-profile
          cp build/libs/*.jar /home/hskgroup-ubuntu/hskgroup-profile/app.jar
          sudo systemctl restart hskgroup-profile
